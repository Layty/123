<UserControl x:Class="三相智慧能源网关调试软件.Views.MsgHostBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border Background="White">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="25" />
                    <RowDefinition />
                    <RowDefinition Height="40" />
                </Grid.RowDefinitions>
                <materialDesign:ColorZone Mode="PrimaryMid" Padding="40"></materialDesign:ColorZone>
                <TextBlock Grid.Row="0" Text="消息窗口" FontSize="15" VerticalAlignment="Center" Margin="5 0 0 0"></TextBlock>
                <StackPanel Grid.Row="1" Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Text" Foreground="{DynamicResource PrimaryHueDarkBrush}" Margin="5"></materialDesign:PackIcon>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>